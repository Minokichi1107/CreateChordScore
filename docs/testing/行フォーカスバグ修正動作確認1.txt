. 修正版editor.jsで動作確認
上記の修正版editor.jsをダウンロードして、js/editor.jsに上書きしてください。
2. 症状の詳細報告
以下を確認して報告してください:
□ 通常モードで行をクリック → 何が起きる？A.何も起きない
□ 通常モードで時刻ボタンクリック → 動作は？
タイムスタンプが入ってる？→入ってる→シークが飛び音楽が再生
→入っていない→行◯の時刻設定モーダルが開く→現在の時刻→何も変わらない→▲▼ボタン時刻が動きタイムスタンプが入力
□ TAPモードで行をクリック → フォーカスは変わる？ok変わる
□ TAPモードで時刻ボタンクリック → 動作は？
タイムスタンプありなし→あり→タイムスタンプが消える
なし→何も反応無し
. 行クリックイベントの所在確認
報告にあったrow.addEventListener('click', ...)のコードがどこにあるか教えてください。
function renderTovLines() {内815行目