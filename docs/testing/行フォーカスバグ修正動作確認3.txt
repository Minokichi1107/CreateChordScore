□ 行の空白部分をクリック
  → その行にフォーカス NG TAPモードのような行フォーカスがされない
  → 歌詞inputにフォーカスが移るok
  → focLineが更新される⇠NGされない
□ 時刻ボタンクリック
  → モーダルが開く（または再生）ok
  → 行クリックイベントは発火しない ok

□ コードタグクリック
  → コード編集モーダルが開くOK
  → 行クリックイベントは発火しない OK

□ 歌詞inputクリック
  → inputにフォーカス ok
  → 行クリックイベントは発火しない ok

□ TAPオーバーレイ内で行クリック
  → tovFocusIdxが更新される　ok
  → トースト表示「行X にフォーカス」ok
  → 通常モードのfocLineに影響しない ▲そもそも通常モードのfocLineが機能不全

□ TAPオーバーレイ内で時刻クリック
  → 時刻が削除される ok
  → 行クリックイベントは発火しない ok